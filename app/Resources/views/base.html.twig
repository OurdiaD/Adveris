<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Centre d'affaires et location de bureaux -  AASbuilding{% endblock %}</title>

    {% block stylesheets %}
        {% stylesheets '@bootstrap_css' %}
            <link rel="stylesheet" type="text/css" media="screen" href="{{ asset_url }}"/>
        {% endstylesheets %}
        <link rel="stylesheet" href="{{ asset('css/style.css') }}" />
    {% endblock %}
</head>
<body>
    {% block header %}    
        <nav class="navbar navbar-inverse navbar-fixed-top">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" href="{{path('homepage')}}">AAS Building</a>
                </div>
                <div id="navbar" class="collapse navbar-collapse">
                    <ul class="nav navbar-nav">
                        <li class="active"><a href="{{path('homepage')}}">Accueil</a></li>
                        <li><a href="{{path('admin')}}">Modifier</a></li>
                    </ul>
                </div>
            </div>
        </nav>
    {% endblock %}
    <div class="container">
        <div class="row">
            {% block nav %}
                <ul class="nav nav-pills nav-stacked col-md-3">
                    <li role="presentation"><a href="#">Secrétariat</a></li>           
                    <li role="presentation"><a href="#">Carte de notre réseau</a></li>            
                    <li role="presentation"><a href="#">Location de bureaux meublés</a></li>
                </ul>
            {% endblock %}

            {% block body %}
                <div class="contain col-md-9">
                    <h1>Mettre un H1</h1>
                    <p>Aller dans modifier et ajouter un li, un h1, ou un p</p>
                </div>
            {% endblock %}
        </div>
    </div>

            {% block footer %}
            <div class=" navbar-fixed-bottom navbar-inverse hidden-xs">
                <div class="footer">
                    Location bureaux | Adhésion réseau | Avantage réseau | Carte de notre réseau
                    Domiciliation commerciale | Formulaire renseignements | Les relais multi-site
                    Les services | Location de bureaux | Notre réseau national | Notre réseau international
                    Permanence téléphonique | Plan du site | Secrétariat | Centre d’affaire
                </div>
                <span class="copyright">© 2010 aas building centres d'affaires et location de bureaux. Tous droits réservés.</span>
            </div>
            {% endblock %}
        
    {% block javascripts %}
        {% javascripts
        '../vendor/jquery/jquery/jquery-1.9.1.min.js'
        '../vendor/twitter/bootstrap/js/dropdown.js'
        %}
        <script type="text/javascript" src="{{ asset_url }}"></script>
        {% endjavascripts %}
    {% endblock %}

    <script type="text/javascript">
        $(document).ready(function() {
            {% block document_ready %}{% endblock %}
        });
    </script>
</body>
</html>
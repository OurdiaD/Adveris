{% extends 'base.html.twig' %}

{% block nav %}{% endblock %}
{% block body %}
    <h1>Page creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
